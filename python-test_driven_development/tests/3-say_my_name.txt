The ``3-say_my_name`` module
==============================

Import the function:

    >>> say_my_name = __import__('3-say_my_name').say_my_name

Regular case with both names:

    >>> say_my_name("Bob", "Lenon")
    My name is Bob Lenon

Using the default value for last_name:

    >>> say_my_name("Bob")
    My name is Bob 

Non-string first_name raises a TypeError:

    >>> say_my_name(12, "Lenon")
    Traceback (most recent call last):
    TypeError: first_name must be a string

Non-string last_name raises a TypeError:

    >>> say_my_name("Bob", 12)
    Traceback (most recent call last):
    TypeError: last_name must be a string

Missing both arguments - calling the function with none of the
name arguments provided at all (even though last_name has a
default, first_name is still required, so this is the "missing
arguments" case for a call with an entirely empty argument list):

    >>> say_my_name()
    Traceback (most recent call last):
    TypeError: say_my_name() missing 1 required positional argument: 'first_name'

    >>> try:
    ...     say_my_name()
    ... except TypeError as e:
    ...     print(e)
    say_my_name() missing 1 required positional argument: 'first_name'
